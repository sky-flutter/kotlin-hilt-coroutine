package com.example.kotlincleanarchitecture.network

import android.service.autofill.UserData
import com.example.kotlincleanarchitecture.network.domain.ApiResult
import retrofit2.http.GET
import retrofit2.http.Query

interface ApiServices {
//    @GET(EndPoints.FETCH_USERS)
//    suspend fun fetchUsers(@Query("page") page: Int): ApiResult<UserData>
}